<form method="post" action="{{ path('scheduled_activity_delete', {'id': scheduled_activity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scheduled_activity.id) }}">
    <button class="btn">Delete</button>
</form>
